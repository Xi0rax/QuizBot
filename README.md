# 🤖 Quiz Bot

Телеграм-бот для прохождения викторины по Python с сохранением результатов и просмотром статистики игроков.

---

## 🔗 Ссылка на бота

**Имя бота в Telegram:** `@py_training_quiz_bot`

---

## 📌 Доступные команды

### `/start`

Запускает бота и отображает приветственное сообщение с кнопкой начала игры.

---

### `Начать игру`

(кнопка или команда `/quiz`)

* Начинает новую викторину
* Сбрасывает предыдущий результат
* Поочерёдно задаёт вопросы

---

### `/quiz`

Альтернативный способ начать викторину без использования кнопки.

---

### `/stats`

Показывает статистику игроков:

* список пользователей
* их **последний результат**
* сортировка по убыванию баллов

Пример вывода:

```
📊 Статистика игроков

1. ID 123456789 — 9 баллов
2. ID 987654321 — 7 баллов
```

---

## 🧠 Логика квиза

* Вопросы задаются последовательно
* Ответ выбирается через inline-кнопки
* После ответа:

    * кнопки удаляются
    * в чат выводится выбранный вариант
    * показывается, верен ли ответ
* В конце викторины выводится итоговый результат

---

## 🗄️ Хранение данных

Используется SQLite (`aiosqlite`).

Для каждого пользователя сохраняется:

* `user_id`
* индекс текущего вопроса
* **последний результат (score)**

---

## 🚀 Запуск проекта

### Локально

1. Установить зависимости:

```bash
pip install -r requirements.txt
```

2. Указать токен бота в `config.py`

3. Запустить:

```bash
python main.py
```

---

### 🐳 Запуск в Docker

1. Собрать образ:

```bash
docker build -t quiz-bot .
```

2. Запустить контейнер:

```bash
docker run -d --name quiz-bot quiz-bot
```

Контейнер будет работать в фоне. Логи можно посмотреть командой:

```bash
docker logs quiz-bot
```

---

## 🐳 Docker

В проекте присутствует `Dockerfile`, позволяющий запускать бота в изолированном контейнере.

Используется образ `python:3.11-slim`. Все зависимости устанавливаются автоматически.


---